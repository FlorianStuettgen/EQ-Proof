
# Spec format (v0.1)
- `variables`: list of variable names
- `units`: map var→unit string (use `"1"` for dimensionless)
- `constraints`: objects of types: `bounds`, `equality`, `sum_leq`, `simplex`, `monotone`
